# 图文生产平台 · 技术交接文档

> 交接日期：2026-09-07 ｜ 代码版本：基线 `ba0ce24` + dsh 创作网关重构
> （当前工作副本无 git 仓库，版本以本文与服务器 `.deployed_commit` 为准）
> 上一版存档：`docs/技术交接文档-20260903.md`（保留不删，Nanobot 时代基线）
> 读者：接手新改进开发的同事。读完本文应能：本地跑起全套环境、理解双路径流水线、
> 安全地改提示词/节点/前端、按流程发版到测试与生产。
>
> 本版主线：**创作网关 Nanobot(:8900) → dsh_serve(:8901)**（DeepSeek Harness 薄层）、
> 提示词库 skill 化（单一事实来源收敛到 `skills/`）、qvp3(:8005) 灰度新栈上线。
> 迁移细节先读 §5.6「dsh 迁移要点」。
>
> ⚠️ **先读第 11 节「服务器红线」——有两台服务器绝对不能动。**
>
> 🌐 在线版（随每次发版自动更新）：`http://117.34.118.115:8003/handover`
> （测试栈 `http://117.34.118.115:8001/handover` 同路径）

---

## 1. 平台是做什么的

面向内容运营的「小红书风格图文」批量生产平台：导入选题 Query → AI 取证/写正文/
分 6 页文案/生成 6 张竖版卡片图（3:4）→ 规则机+视觉审核 → 人工核查/审图 → 签发导出。
三种生产模式：`general`（通用科普）、`single`（单一产品实测，图生图保外观）、
`compare`（两主体对比，双实景参考图生图）。

设计原则：**AI 只做创作段，业务调度、校验、审核、记账全部在后端确定性代码里**；
每个 LLM/生图调用可 mock、可重试、有幂等键、有成本记账。

## 2. 总体架构

```
浏览器（Vue3 CDN 版 SPA，hash 路由，无构建步骤）
   │  HTTP + SSE
   ▼
FastAPI 后端（uvicorn，单进程 asyncio）         Docker Compose（每套栈独立）
   ├─ scheduler：进程内 asyncio 队列 + 自适应并发限流        ┌─────────────────┐
   ├─ 双路径流水线 orchestrator                              │ qvp2-app  :8003 │ 生产（Nanobot 栈）
   │    ├─ Agent 路径（生产默认，AGENT_PIPELINE_ENABLED=true） │ qvp2-postgres   │
   │    │     └→ 创作网关（OpenAI 兼容 API，二选一）          │ qvp2-openserp   │
   │    │           ├─ qvp2 栈：Nanobot :8900（容器内拉起）    └─────────────────┘
   │    │           └─ qvp3 栈：dsh_serve :8901               ┌─────────────────┐
   │    │                Python FastAPI 薄层，内嵌 dsh 子进程  │ qvp3-app  :8005 │ dsh 灰度新栈
   │    │               （DeepSeek Harness 0.1.2-rc.1 钉版，   │ qvp3-postgres   │ 2026-09-07 上线
   │    │                 协议与 Nanobot 1:1，后端改           │ qvp3-openserp   │ 公网可达
   │    │                 NANOBOT_BASE_URL 指向即可切换）      └─────────────────┘
   │    │                 └→ 创作大节点 LLM（三级 failover：flash 主→kimi-k3 备1→Kimi Code k3 备2，薄层自做）
   │    │                       └→ MCP 工具（stdio 子进程 qvp_mcp）
   │    └─ 直连 13 节点路径（兜底开关，秒切回）
   ├─ 视觉审核 visual_check（dashscope qwen-vl-max）
   ├─ rule_check / cross_check / risk_classify（纯代码，无 LLM）
   └─ PostgreSQL 16（SQLAlchemy async + asyncpg）＋ Redis（限流/心跳）
```

一次 Agent 路径生产的数据流：
导入 → 文字核查关卡（人工放行）→ 实景搜图关卡（人工筛选，compare/single 才有）→
`agent_production` 一次调用创作 Agent（Nanobot 或 dsh_serve，协议一致；内部用
MCP 工具：豆包搜索取证 → 生图 gpt-image-2 → 逐页 OCR 自检）→ 后端收契约 JSON
做确定性收尾（图片本地化/去重/尺寸校验/落库 → 规则机 → 交叉核验 → 风险分级 →
人工审图 → 会签 → 快照）。

## 3. 技术栈清单

| 层 | 技术 | 说明 |
|---|---|---|
| 后端 | Python 3.12 + FastAPI + uvicorn | 单进程 asyncio；`src/config.py` pydantic-settings 读 `.env` |
| ORM/DB | SQLAlchemy 2 async + asyncpg + PostgreSQL 16 | 迁移是编号 SQL（`migrations/001-021`），entrypoint 的 `init_db.py` 幂等应用 |
| 费率核算 | `model_rates` 表 + `src/gateway/cost_tracker.py`（020/021，2026-09-09） | 费率 DB 权威、改库/管理页保存即生效（进程内缓存 60s+代码兜底）；DeepSeek 按缓存命中/未命中分段计价，北京时间工作日 9-12/14-18 点高峰、空闲半价；生图分通道按次计费（`gpt-image-2@<channel>` 行，fusion=0.2 元/张账单实证，无通道行回退基准行→`IMAGE_COST_PER_IMAGE_CNY` 兜底）；费用四类拆分 text_llm/image_gen/search/ocr（节点名优先、模型名兜底）；管理页 `GET/PUT /api/admin/rates`、`GET /api/admin/balance`（DeepSeek 余额 API 实拉 + FusionAI/Kimi 基准录入扣减估算 + `PUT /api/admin/balance_baseline`） |
| 缓存/限流 | Redis 5 | 自适应并发限流器、审核心跳锁 |
| LLM 文本 | LiteLLM 适配，DeepSeek flash 主 / Kimi 开放平台 kimi-k3 备1 / Kimi Code k3 备2（KIMI_CODE_API_KEY 空则禁用） | `src/gateway/failover.py::call_with_failover`，流式回调进度 |
| 创作网关 | **dsh_serve（FastAPI 薄层 :8901）+ dsh（DeepSeek Harness 0.1.2-rc.1 钉版）** | OpenAI 兼容协议与 Nanobot 1:1；session 隔离/SSE/failover 主备/并发信号量/MCP 挂接/会话冷恢复 fork 全在薄层（dsh_serve/README.md）；Nanobot(:8900) 保留作回退 |
| 提示词资产 | **`skills/` 目录（10 个 skill 包）+ `src/gateway/skill_loader.py`** | 全部生成类提示词的单一事实来源；`page-split/contract.txt` 为字数契约单点（详见 5.3） |
| Node 运行时 | node:24-slim 复制进应用镜像（dsh CLI 需要） | glibc 必须与 python:3.12-slim（Debian）匹配，**勿用 alpine（musl libc，跑不起来）**；SDK 形态则自带运行时无需系统 Node |
| Agent 工具 | 自研 MCP server `qvp_mcp`（stdio 子进程） | web_search / image_search / image_gen / ocr，按 task_id 硬配额+成本回调 |
| 生图 | gpt-image-2，三通道轮询 | FusionAI（主）/LinkAI/Moacode，`IMAGE_GEN_CHANNELS`；画质档 `high` |
| 实景搜图 | OpenSERP（docker 容器） | `ref_collect` 搜≥10张 → OCR 初筛 → 人工关卡 |
| 视觉审核 | dashscope qwen-vl-max | `src/services/visual_check.py`，图文一致性/主体匹配/错字 |
| 前端 | Vue3 + vue-router（CDN 全局构建，**无 npm/无打包**） | `static/` 下原生 ES 模块 JS，hash 路由 `/#/tasks` |
| 测试 | pytest + pytest-asyncio | 单测+集成（mock 全部外部调用），真实 PG |
| 部署 | git archive → scp → docker compose up -d --build | 服务器 Docker Hub 不通，镜像全部本地已有；qvp3 栈走镜像本地化（docker save→scp→docker load，见 §9） |

## 4. 代码仓库结构

```
code/
├─ src/
│  ├─ api/            FastAPI 路由（tasks/review/admin/stream/auth/dashboard…）
│  ├─ pipeline/       流水线核心
│  │   ├─ orchestrator.py   NODES（直连13节点）/ NODES_AGENT（10节点）+ run_pipeline
│  │   ├─ nodes.py          execute_node（三段式短事务：幂等/SSE/取消删事件，见 §14）
│  │   ├─ agent_production.py  创作大节点：组装提示词→call_agent→契约校验→落库
│  │   ├─ text_check.py     文字核查节点（起草正文/图上文案草稿，独立提示词）
│  │   ├─ idempotency.py    节点事件幂等（同输入跳过，失败可重跑）
│  │   └─ scheduler 在 src/stream/scheduler.py（asyncio 队列/限流/恢复）
│  ├─ gateway/        外部调用网关
│  │   ├─ failover.py       call_with_failover（LLM 主备切换，流式 on_delta）
│  │   ├─ nanobot_client.py call_agent（SSE 聚合，session_id=任务级隔离；
│  │   │                     协议 1:1，NANOBOT_BASE_URL 可指 Nanobot 或 dsh_serve）
│  │   ├─ skill_loader.py   ★skills/ 取词器（SKILL.md frontmatter/片段/列表片段/
│  │   │                     字数契约常量；首读落缓存，测试可 reload()）
│  │   ├─ image_gen.py      gpt-image-2 三通道轮询（生成+编辑/图生图）
│  │   ├─ image_search.py / web_search.py / ocr.py
│  │   ├─ prompt_versions.py 提示词库门面（常量名/三级覆盖语义不变，内部改从
│  │   │                     skill_loader 读；演进注释仍在）
│  │   ├─ tool_ledger.py    MCP 工具成本台账
│  │   └─ cost_tracker.py / litellm_adapter.py
│  ├─ services/       业务服务（style_select 风格两级库 / regen 定点重生成 /
│  │                   visual_check 视觉审核 / visual_writer 实景构图 / combo 组合导入）
│  ├─ stream/         bus.py（进程内 SSE 总线）/ scheduler.py / limiter.py
│  ├─ models/         SQLAlchemy 模型（tasks/drafts/assets/entities/review/events…）
│  └─ config.py       全部配置项（pydantic-settings，env_file=.env）
├─ dsh_serve/         ★dsh 创作网关薄层（OpenAI 兼容端点 / session 隔离 / SSE 字段映射 /
│                     failover 主备 / 并发信号量 / MCP 挂接 / 会话冷恢复 transcript fork；
│                     README.md 有配置全表；analyze_session.py 会话日志分析、
│                     verify_contract.py 契约冒烟；tests/ 34 项单测）
├─ skills/            ★提示词资产单一事实来源（2026-09-03 阶段2重构）：
│                     10 个 skill 包——draft-write / page-split / image-prompt /
│                     text-draft / page-regen / page-subject / visual-writer /
│                     ai-review / prompt-analyze / bench-spec；
│                     page-split/contract.txt = 图上文案字数契约单点
├─ qvp_mcp/           MCP 工具 server（15 个工具：4 素材 + 11 能力，见 5.7；
│                     配额+成本 HTTP 回调，stdio 传输）
├─ static/            前端（index.html + api.js + components/ + views/，无构建）
│                     + handover.html（交接文档在线版，由脚本生成勿手改）
├─ migrations/        001-019 编号 SQL 迁移（全部幂等，init_db.py 启动时应用）
├─ tests/             unit/ + integration/（集成测试用真实 PG，mock LLM/Agent）
├─ scripts/           开发辅助脚本（smoke_* 冒烟 / verify_* 校验 / build_handover.py /
│                     start-all-dsh.sh dsh 版一键启停 /
│                     verify_skill_equivalence.py 提示词重构等价性验证）
├─ deploy/            部署资产：Dockerfile.dsh（qvp3 镜像，node:24-slim 复制 Node）/
│                     entrypoint.dsh.sh（qvp3 容器入口：init_db→dsh_serve 守护→后端）/
│                     compose/qvp3.docker-compose.yml（8005 dsh 栈）/ 各环境配置
├─ docs/              历史设计文档与训练记录（改造说明-Nanobot全链Agent.md、
│                     dsh-spike-验证记录.md 必读）
├─ nanobot/           nanobot 配置（config.json：provider/preset/fallback/MCP 声明）
│                     + start-nanobot.sh（从 .env 导出密钥后启动 :8900；回退用）
├─ docker-compose.yml qvp2 独立栈（postgres 不对宿主机开端口，仅 app:8003 对外）
├─ .env.example       环境变量模板（真实 .env 不入库）
└─ requirements.txt   uv 导出的锁定依赖（69 个顶层包）
```

## 5. 核心机制详解

### 5.1 双路径流水线（最重要的设计）

`src/pipeline/orchestrator.py`：
- `NODES`（直连 13 节点，定义在 nodes.py:31）：task_import → entity_bind →
  evidence_build → draft_gen → rule_check → page_split → asset_gen → ocr_read →
  cross_check → risk_classify → review_queue → batch_signoff → publish_snapshot
- `NODES_AGENT`（Agent 路径，**生产默认**）：创作段 6 节点（entity_bind/evidence_build/
  draft_gen/page_split/asset_gen/ocr_read）收敛为 1 个 `agent_production`，并新增
  ref_seed / text_check / ref_collect 三个关卡节点：task_import → ref_seed →
  text_check → ref_collect → agent_production → rule_check → cross_check →
  risk_classify → review_queue → batch_signoff → publish_snapshot
  （**2026-09-07 两段式实景搜图**，吸收 8002 分叉栈设计：
  ①ref_seed（`src/pipeline/ref_seed.py`，自动、无人工关）素材库复用+按主体搜图
  +OCR 初筛（复用 `ref_collect._collect_candidates`），落 `selection_status='text_ref'`
  反哺 text_check 起草（`text_check._refs_feedback_section` 注入段内容/位置不变，
  无图零差异）；②text_check 人工关（awaiting_text）；③ref_collect 搜图②：
  候选池=text_ref 叠加新搜（内容 hash 去重，text_ref 全部并入 candidate 不留
  残留），人工确认关 awaiting_refs，keep→confirmed、其余→rejected；
  ④agent_production 读 confirmed 不变（refs_section 含「必须使用+图生图+
  文案与参考图画面一致」）。确认后幂等续跑机制不变；general 模式仍由
  `REF_FOR_GENERAL_ENABLED`（默认 false）门控，false 时 ref_seed/ref_collect
  照旧跳过、整条链路零差异。前端节点条由 /api/meta/nodes 读
  `progress.node_order()`，自动跟随。）
- 开关 `AGENT_PIPELINE_ENABLED`（.env），false 秒切回直连路径兜底。
- 两路径**同源提示词**：都从 `get_effective_prompt(kind, mode, owner_id)` 取词
  （解析顺序：用户自定义 → admin 系统覆盖 → 代码默认）。
  ⚠️ 历史教训：同一要求必须同时注入所有路径（详见 5.3）。

### 5.2 三道人工关卡

| 关卡 | 状态 | 放行 API |
|---|---|---|
| 文字核查（正文/页文案草稿人工核改） | `awaiting_text` | `POST /api/tasks/{id}/text/confirm`（另有 redraft/reject） |
| 实景参考图确认（compare/single） | `awaiting_refs` | `POST /api/tasks/{id}/refs/confirm`（keep_ids） |
| 审图（单角色通过即生效） | `review` | `POST /api/review/action`（approve/reject+定点标记） |

关卡挂起后任务回队或等待，确认后**幂等续跑**（已完成节点跳过）。
人工驳回带定点标记（page/image+理由）→ `rejected` → retry 触发 `partial_regen`
（只重生成标记项；意见注入提示词，`src/services/regen.py`）。

### 5.3 提示词库体系（改提示词必读）

**2026-09-03 阶段2重构后：全部生成类提示词的单一事实来源是 `skills/` 目录**
（10 个 skill 包），由 `src/gateway/skill_loader.py` 统一取词；
`prompt_versions.py` 常量名与取值不变（内部改从 skill_loader 读），
三级覆盖语义（用户自定义 → admin 系统覆盖 → 代码默认）原样保留。关键点：

- skill 包结构：`skills/<name>/SKILL.md`（frontmatter：name/description/notes +
  正文模板）+ 片段文件 `<frag>.txt`（可带一层子目录，如 `prompts/general.txt`）；
  列表型片段（如 6 条排版轮换）单文件内以单独一行 `---` 分隔，
  用 `fragment_list()` 读取。
- 正文写作的「人设与真人感+信息密度」共享段（`skills/draft-write/shared_persona.txt`）
  必须**三条路径都注入**：①直连 draft_gen（nodes.py）②Agent 路径
  （agent_production.py 取词后 append）③text_check 起草（用户在核查关卡看到的草稿）。
  注入条件不变：`template == default_prompt(...)` 时才追加——用户自定义模板代表
  显式意图，不覆盖（防负优化）。
- 图上文案字数契约（2026-09 起）：每页 **80-130 字**、六页差≤40、每页 1-2 个具体
  信息点。**已收敛为单点 `skills/page-split/contract.txt`**（PAGE_MIN_CHARS 等
  5 个键，skill_loader 顶部解析成常量）——page-split/prompt-analyze 提示词、
  `nodes._balance_issue` 硬校验（不达标自动带意见重试一次）、
  `agent_production._AGENT_INSTRUCTIONS` 合规红线段全部从该单点渲染，连同
  `regen.py` 定点重写的均衡参照段共 8 处消费方同吃一源
  （血泪史：此前要 7 处手工同步，漏一处就出阴阳口径）。**改字数口径只改
  contract.txt**，并跑 `tests/unit/test_page_balance.py` 守卫断言。
- 提示词类重构的兜底验证：`scripts/verify_skill_equivalence.py`——重构前
  `--baseline`（快照全部取词/渲染结果），重构后 `--verify`（逐键对比零差异
  退出码 0，另对撞 contract.txt 解析出的契约常量）。顺序不可颠倒。
- 测试里有「守卫断言」锁定关键提示词片段（如 test_absorb_8002 锁「胶囊」
  「CAPSULE」、test_page_balance 锁字数）——改提示词先跑相关测试。

### 5.4 生图与视觉闭环

- 生图走 MCP `image_gen` → `src/gateway/image_gen.py`：`IMAGE_GEN_CHANNELS`
  通道轮询（fusion,linkai,moacode），`IMAGE_GEN_PARALLEL=6` 并发，size 1152x1536
  （3:4），quality=high；MOCK_IMAGE_GEN=true 时返回占位图（联调用，不花钱）。
- compare/single 模式：确认后的实景参考图作为图生图 references 传入，
  **6 页实景用法必须错开**（按页轮播分配，铁律，e6c550b）。
- 落库后：本地化/去重/hash/尺寸校验 → OCR 逐页比对（错字/漏字自动重生成，
  garble_regen loop）→ qwen-vl-max 视觉审核（图文一致/主体匹配 `subject_mismatch`）。

### 5.5 实时流与记账

- `src/stream/bus.py` 进程内事件总线 → `/api/stream/events`（SSE）：节点开始/完成/
  失败（含 traceback）、Agent 流式增量、生图进度。前端监控页实时渲染。
- 成本：`tool_ledger` + `cost_tracker`，节点事件记 `cost_estimate_cny`，
  `/api/admin/costs` 汇总。Agent 工具成本经 HTTP 回调合并进节点。

### 5.6 dsh 迁移要点（Nanobot → dsh_serve，2026-09 重构）

完整验证记录见 `docs/dsh-spike-验证记录.md`（Go/No-Go 七项 + 三轮真实链路补测），
组件细节与配置全表见 `dsh_serve/README.md`。接手必记的几条：

1. **薄层形态**：dsh（DeepSeek Harness）没有原生 OpenAI 兼容端点（`dsh web` 是
   浏览器 GUI 宿主，headless 不开端口），dsh_serve 是 Python FastAPI 薄层，经官方
   SDK 常驻驱动 `dsh --profile sdk` 子进程。`POST /v1/chat/completions` +
   `session_id` + SSE（delta.content / delta.reasoning_content / 末尾 usage chunk）
   与 Nanobot 1:1——后端 `nanobot_client` 只改 `NANOBOT_BASE_URL` 指向 :8901 即切换。
2. **max_tokens 含 reasoning_tokens（本轮最大的坑）**：deepseek 推理模型的输出
   预算把推理 token 算在内——旧值 8192 在 reasoningEffort=high + 长 prompt 下被
   推理一轮吃光：正文 0 字符、0 次工具调用，`turn/end reason=max-tokens`
   （2026-09-05 真实任务事故，会话日志铁证）。现 `DSH_MAX_TOKENS=32768`；
   max-tokens 截断是显式失败形态（TruncatedOutputError）触发主备降级。
   再遇 max-tokens 降级：先调低 `DSH_REASONING_EFFORT` 或继续上调上限。
3. **跨进程会话恢复靠 transcript 镜像 + 别名 fork**：dsh sdk profile 不支持跨进程
   冷恢复（重启后同 session_id 会被持久化层以 "id collision" 拒绝）。薄层每轮
   成功后在 `$DSH_HOME/dsh-serve/transcripts/` 记录 user/assistant 正文，撞碰撞时
   自动 fork 新别名 `<sid>--r<N>`、把历史作前言拼进 prompt 重发（碰撞在 turn
   开始即失败，不耗 token，调用方无感知）。**历史只保留最近 20000 字符**——
   纠错重问（近几轮）完全够用，跨长任务多节点编排记忆不保证完整。
4. **MCP 冷启动 52-88s**：qvp_mcp import 慢（litellm 远程价表拉取 + OneDrive 云盘
   IO），逼近/超过 MCP SDK 60s 握手上限，首挂靠自动重连 1-2 分钟内补上。缓解：
   `MCP_EXTRA_ENV` 里加 `LITELLM_LOCAL_MODEL_COST_MAP=true`，并把
   `DSH_INITIALIZE_TIMEOUT_SECONDS` 提到 120（默认已是）。
5. **内置 web_search 已禁用**：dsh base 自带 web_search/web_fetch，不显式点名时
   模型会用错工具（补测实证）。serve patch 默认禁用（`tool-web` 行 disabled），
   检索/生图/OCR 只存在 `mcp__qvp__*` 形态；生产 prompt 里也要明确点名工具名。
6. **钉版纪律**：dsh 处于 developer preview，官方明示 "THERE WILL BE
   COMPATIBILITY-BREAKING CHANGES"——版本钉死 **0.1.2-rc.1**
   （deploy/Dockerfile.dsh 的 `ARG DSH_VERSION`），升级前必须重跑 spike 验证清单
   并回归 dsh_serve 单测。
7. **failover 薄层自做（2026-09-10 起三级）**：dsh 原生只有同路由重试
   （max 5 次），不跨模型切换。三级线路：**主 DeepSeek 官方 deepseek-v4-flash
   （价约 pro 1/3）→ 备1 Kimi 开放平台 kimi-k3（openai-completions，
   api.moonshot.cn，按量付费）→ 备2 Kimi Code 会员 k3（anthropic-messages，
   api.kimi.com/coding；`KIMI_CODE_API_KEY` 空则该级自动禁用）**。主路由失败
   （连接错误/子进程崩溃/超时/max-tokens 截断/空响应/拒答词命中）时同 session
   逐级整轮重发。流式纪律：reasoning 实时透传，content/usage 缓冲到当轮质检
   通过才 flush——客户端永不见半截正文。代价：降级轮首 token 延迟 = 前级整轮
   耗时（结构性成本，暂接受）。后端直连路径 `src/gateway/failover.py` 同为三级
   （`kimi_code_api_key` 默认空=禁用第三级）。
8. **profiles/node_modules 半成品处置**：主备两个 dsh 子进程并发首启竞争
   `$DSH_HOME/profiles/node_modules.lock`（组件内已加 `_boot_lock` 串行化）；
   首启超时被强杀后 `profiles/node_modules` 留半成品，下次启动报
   "not a dsh-managed module proxy"——**删掉该目录（或整个 dsh-home）即恢复**。
9. **容错语义对齐 Nanobot**：MCP 挂不上不阻断启动（`failOnStartupError:false`，
   模型侧感知降级）；`/health` 的 `mcp.command_exists` 可事前发现 MCP_COMMAND
   路径配错。qvp_mcp 的配额/成本回调是 fail-open——后端不在时工具照常返回。

### 5.7 能力工具目录（功能项独立化，2026-09-07）

设计一句话：**每个功能小项 = 一个可独立调用的 MCP 工具（带配额+成本记账）+
`skills/` 对应文档**；流水线行为不变，MCP 层是 `src/` 同实现的薄包装。

| 工具名 | 功能 | 包装来源 | 配额 |
|---|---|---|---|
| `draft_write` | 正文起草 | `nodes.run_draft_gen` | 5 次 |
| `page_split` | 分页+字数均衡校验 | `nodes.run_page_split_llm` + `page_balance_issue` | 5 次 |
| `page_regen` | 定点重写页文案 | `services/regen.rewrite_page_copy` | 5 次 |
| `visual_write` | 实景构图描述 | `services/visual_writer` | 5 次（成本暂记 0） |
| `text_draft` | 起草正文/图上文案草稿（核查关用） | `pipeline/text_check` | 5 次（纯计算不写库） |
| `prompt_analyze` | 组合导入提示词分析 | `services/combo` | 5 次 |
| `page_subject` | 页主体提取 | `services/page_subject` | 5 次 |
| `rule_check` | 规则机校验 | `quality/rules` | 10 次 |
| `cross_check` | 交叉核验 | `quality/cross_check` | 10 次 |
| `risk_classify` | 风险分级 | `risk/classifier` | 10 次（纯函数显式输入） |
| `visual_check` | 视觉审核 | `services/visual_check` | 10 次 |

素材类 4 个原有工具（web_search / image_search / image_gen / ocr）不变。

统一规约：
- 返回结构统一为 `{"ok", "data", "error"}`。
- 配额分两类，配置项 `mcp_max_llm_tools_per_task`（LLM 类，默认 5）/
  `mcp_max_check_tools_per_task`（校验类，默认 10），按 task_id 硬限。
- skill 文档对应关系：每个工具在 `skills/<name>/SKILL.md`（或同目录
  `MCP.md` 的「独立调用」章节）有对应文档。

实测结论：dsh 侧三个微任务（page_split / rule_check / draft_write）全部一次
选对工具，配额+成本回调真实落账（证据在 `docs/dsh-vs-nanobot-对比分析.md`
第五节）。

## 6. 外部服务与密钥（.env）

| 变量 | 用途 |
|---|---|
| `DATABASE_URL` | PG 连接（本地 5433 / 服务器容器内） |
| `DEEPSEEK_API_KEY` / `KIMI_API_KEY` | 文本 LLM 主/备（dsh_serve 主备路由也用这两个） |
| `OPENAI_IMAGE_*` / `MOACODE_*` / `FUSIONAI_*` | gpt-image-2 三通道 |
| `IMAGE_GEN_CHANNELS` | 通道轮询顺序（⚠️ 当前 .env 只写了 fusion，无备胎） |
| `DASHSCOPE_API_KEY` | qwen-vl-max 视觉审核 + OCR |
| `AGENT_PIPELINE_ENABLED` | 双路径开关 |
| `NANOBOT_BASE_URL` | 创作网关地址（Nanobot 栈 `http://127.0.0.1:8900/v1`；dsh 栈 `http://127.0.0.1:8901/v1`，协议 1:1） |
| `MOCK_IMAGE_GEN` | 生图 mock 开关（联调必备） |

dsh_serve 自身配置（`DSH_*` / `MCP_*` / `FALLBACK_ENABLED`，全环境变量）见
`dsh_serve/README.md` 配置全表；服务器侧在 compose environment 里注入。

## 7. 本地开发环境搭建

Windows（Git Bash）实测步骤：

```bash
cd code
python -m venv .venv && .venv/Scripts/python -m pip install -r requirements.txt
# 1) 起本地 PG(5433)+Redis（docker run 或 compose 里的依赖服务）
# 2) cp .env.example .env 填好密钥；本地库连接串用 5433
DATABASE_URL="postgresql+asyncpg://qvp:qvp@localhost:5433/qvp" \
  PYTHONUTF8=1 .venv/Scripts/python init_db.py    # 幂等建表+迁移（仓库根目录）
# 3) 起后端（注意必须用 .venv 的 python，系统 python 没装依赖）
DATABASE_URL="postgresql+asyncpg://qvp:qvp@localhost:5433/qvp" PYTHONUTF8=1 \
  .venv/Scripts/python -m uvicorn src.api.main:app --port 8003
# 4) 起创作网关，二选一：
#    a) Nanobot（会从 .env 导出密钥，MCP 由它以 stdio 子进程拉起）
bash nanobot/start-nanobot.sh
#    b) dsh 版一键启停（PG → dsh_serve:8901 → 后端:8003，后端
#       NANOBOT_BASE_URL 自动指向 dsh_serve；含 stop/status 子命令）
bash scripts/start-all-dsh.sh
# 5) 跑测试
DATABASE_URL="postgresql+asyncpg://qvp:qvp@localhost:5433/qvp" PYTHONUTF8=1 \
  .venv/Scripts/python -m pytest tests/ -q
```

日常联调建议 `MOCK_IMAGE_GEN=true`（生图免费秒回）。真实出图约 1-2 分钟/张，
一条任务全链 10-25 分钟。

**改动后必须重启进程**：后端（uvicorn）读代码在启动时；MCP 子进程继承创作网关
（Nanobot 或 dsh_serve）的环境与配置快照——改 `.env` 后后端和创作网关**都要重启**，
否则 MCP 还用旧值（实际踩过：MOCK 开关改了没生效，因为只重启了后端）。

## 8. 测试体系

- `tests/unit/`：提示词守卫（字数/胶囊/CAPSULE/人设段等）、均衡校验、纯函数。
- `tests/integration/`：全流程。mock 手法：
  - Agent 路径：`agent_path` fixture patch `nanobot_client.health/call_agent`
    返回契约 JSON（test_agent_pipeline.py，照抄即可加用例）；
  - 直连路径：patch `call_with_failover` 返回固定文本/JSON；
  - 生图：SVG data-uri mock，验证落库/去重/hash。
- dsh_serve 组件单测在 `dsh_serve/tests/`（34 项，含 max-tokens 截断降级、
  半截正文不出流、patch 生成等）。**已纳入主回归**（2026-09-07：pyproject
  `testpaths=["tests","dsh_serve/tests"]`，裸跑 `pytest -q` 即 291 项全量）；
  注意 venv 重建需装 `deepseek-harness-sdk==0.1.2rc1`（已在 requirements.txt）。
- 跑法见上节命令；单个文件约 1-5 分钟。**改提示词/节点后至少跑**
  `test_page_balance + test_absorb_8002 + test_agent_pipeline + 相关集成`。

## 9. 发版流程（8003 → 8001，qvp3 走镜像本地化）

qvp2/qvptest（Nanobot 栈）：

```bash
cd code && git archive --format=tar.gz -o /tmp/deploy.tar.gz HEAD
scp -i <deploy密钥> /tmp/deploy.tar.gz th@117.34.118.115:/home/th/qvp2/deploy.tar.gz
ssh th@117.34.118.115 "cd /home/th/qvp2 && tar xzf deploy.tar.gz && \
  echo <commit短hash> > .deployed_commit && docker compose up -d --build"
# 测试栈同步（rsync→rebuild→风格SQL，一键）：
ssh th@117.34.118.115 "bash /home/th/qvp-test/sync_from_qvp2.sh"
# 健康检查：curl :8003/api/tasks?limit=1 与 :8001 同
```

qvp3（dsh 栈，8005）——**git tag 发版流（2026-09-10 起，替代 tar 包）**：

> ⚠️ **qvp3 是完全独立栈：不参与 `sync_from_qvp2.sh`，不与 8003/8001 同步任何
> 代码/数据/配置**（用户决策 2026-09-07）。它有自己的代码目录、数据卷、记忆会话，
> 发版只走本节下方的独立流程。

**版本仓库**：服务器本地 bare 仓 `ssh://th@117.34.118.115/home/th/qvp3-repo.git`
（2026-09-10 建，含交接包起的完整历史；密钥与运行时产物永不入库）。
本地 push 需 deploy 密钥：`GIT_SSH_COMMAND="ssh -i <deploy密钥> -o BatchMode=yes" git push`。

```bash
# 1) 本地：改动入库 → 全量回归绿 → 打 tag → 推送
cd code && git add -A && git commit -m "<本次改动摘要>"
git tag -a "v<N>-<yyyymmdd>" -m "<版本摘要>" && git push origin main --tags

# 2) 服务器：按 tag 出码（替代旧 tar 包流程）+ 镜像更新
ssh -i <deploy密钥> th@117.34.118.115 "
  git --git-dir=/home/th/qvp3-repo.git archive v<N>-<yyyymmdd> | tar -x -C /home/th/qvp3"
# 镜像仍走本地化（Docker Hub 不通）：本地 build → docker save → scp → 服务器 load
# 代码未变仅配置/数据变更时可跳过镜像，直接 compose up -d
# ⚠️ load 后必须补 docker tag（v12-20260914 实测踩坑）：save 的 tar 只带版本 tag，
#    compose 引用 qvp3-app:latest，不打 tag 时 up -d 不触发重建（旧容器照跑）
ssh -i <deploy密钥> th@117.34.118.115 "cd /home/th/qvp3 && \
  docker tag qvp3-app:v<N>-<yyyymmdd> qvp3-app:latest && \
  echo v<N>-<yyyymmdd> > .deployed_commit && \
  docker compose -f deploy/compose/qvp3.docker-compose.yml --project-directory . up -d"
# 健康检查：curl :8005/api/tasks?limit=1 与 :8005/handover
# （qvp3-app 的 Status 应从「Up N 天」变为「Up About a minute」+ 库内数据校验）
```

- 基座镜像同理本地化：`docker save node:24-slim` / `docker save postgres:16` →
  scp → 服务器 `docker load`（Dockerfile.dsh 头部注释有写）。
- 迁移随启动幂等应用，无需手工执行。
- 技术交接文档在线版随包自动更新：改 `docs/技术交接文档-*.md` 后运行
  `.venv/Scripts/python scripts/build_handover.py` 重新生成 `static/handover.html`
  并一并提交，线上 `/handover` 即为最新版（脚本自动取文件名最新的一篇，
  新日期文档无需改脚本）。
- `.deployed_commit` 现在写 **tag 名**（如 v11-20260910），与 git tag 一一对应。
- 服务器 Docker Hub 不通：镜像（postgres:16 等）全部用服务器本地已有镜像，勿改 tag。

**旧 tar 包流程存档（勿执行）**：git archive HEAD → scp deploy.tar.gz → 解压，
已被上方 tag 流替代（2026-09-10 起）。

## 10. 服务器布局

| 端口 | compose 项目 | 目录 | 用途 |
|---|---|---|---|
| 8005 | qvp3 | /home/th/qvp3 | **dsh 创作网关新栈（本项目唯一负责）**，2026-09-07 上线，公网可达 |
| 8003 | qvp2 | /home/th/qvp2 | ⛔ 生产栈，**2026-09-08 起已移交同事**，本项目不动 |
| 8001 | qvptest | /home/th/qvp-test | ⛔ 测试栈，**2026-09-08 起已移交同事**，本项目不动 |
| 8002 | qvpdev | — | ⛔ 同事分叉开发栈，**绝不 restart/build/rm** |
| 8000 | qvp（旧栈） | — | ⛔ 工程师马瑞辰的栈，**绝对不动** |

## 11. 服务器红线（最高优先级）

1. **8003、8001、8002、8000 四台栈全部不动**（不 restart、不 build、不 down、
   不 rm、不进容器、不改文件）：8002/8000 是历史红线；**8003/8001 已于
   2026-09-08 移交同事**（本地 8003 原生栈同理，只读参考用）。
2. **本项目开发只允许同步两个目标：本地 8005 与服务器 8005（qvp3）**。
   发版门禁（2026-09-08 用户决策）：dsh 改动一律先更新本地 8005
   （`docker compose -f deploy/compose/qvp3.docker-compose.yml --project-directory . up -d`
   或 docker cp 热更新 static/）→ 本地验证 → 用户确认 → 才走 §9 镜像本地化
   流程更新服务器 8005。
3. 服务器命令只作用于 `/home/th/qvp3` 一个目录。
4. 历史存档（勿执行）：2026-09-08 前曾有 qvp2/qvp-test 的发版与同步流程
   （§9 上方 qvp2/qvptest 段落），移交后仅作历史参考。

## 12. 数据模型速览（关键表）

- `tasks`：状态机 draft/processing/awaiting_text/awaiting_refs/review/rejected/
  approved/failed/cancelled；`text_review`（起草 JSON）、`text_override`（人工核定版）、
  `gen_style`/`gen_category`（风格自适应回填）。
- `drafts`（正文多版本）、`page_copies`（6 页图上文案，body 列）、
  `assets`（source_type: ai_generated/official；selection_status: candidate/confirmed/
  rejected；subject_mismatch）、`ocr_results`（confidence）、
  `claims`/`evidences`（取证）、`node_events`（幂等+记账+error_class）、
  `review_sessions`/`review_actions`/`approvals`/`reject_marks`（审核与定点驳回）、
  `style_keywords`（两级风格库，owner_id NULL=公共）、`prompt_library`（提示词库覆盖）、
  `activity_logs`、`issue`、`publish_snapshots`、
  `model_rates`（020/021 动态费率表：高峰/空闲/缓存命中单价+分通道按次价，管理页可改即生效）、
  `balance_baselines`（021 余额基准点：无公开余额 API 的厂商 fusion/kimi 由管理员
  抄录控制台余额，台账类别消耗扣减估算当前余额）。
- ⚠️ 列名易错点（实测踩过）：风格描述在 `style_keywords.description`；
  资产类型是 `source_type` 不是 asset_type；页文案列是 `body` 不是 page_text；
  activity_logs 的列是 `action`/`detail`，tasks 没有 current_node 列（current_node 是
  API 从 node_events 推导的）。

## 13. 前端（无构建 SPA）

- `static/index.html` 引 Vue3/vue-router 的 vendor 文件 + `api.js` +
  `components/{Layout,StepsBar,Lightbox}.js` + `views/{Login,Dashboard,Import,Tasks,
  TextCheck,RefsBoard,Review,Monitor,Admin,Settings,Users,Sample}.js`。
- hash 路由：`/#/tasks` 这种 `#` 后内容不会发到服务器；若浏览器把 `#` 编码成 `%23`
  会得到后端 404（用户报过「打不开」，实为链接里的 %23）。
- 改前端直接改 JS 文件，刷新即生效；版本戳用 `?v=YYYYMMDDx` 控制缓存。
- 实时数据走 SSE `/api/stream/events`（监控页事件流/节点进度）。

## 14. 已知坑与经验（血泪史精选）

1. **Windows 本地**：必须用 `.venv/Scripts/python.exe`（系统 python 没装依赖）；
   Git Bash 调 curl 上传文件用相对路径（`/tmp` 路径转换会失败）；中文 query 参数要
   URL 编码。仓库部分文件 CRLF，git 会警告，无碍。
2. **MCP 环境缓存**：`qvp_mcp` 是创作网关的 stdio 子进程，读 `.env` 发生在网关
   启动拉起子进程那一刻——改配置后「重启后端」不够，要重启创作网关
   （Nanobot/dsh_serve 同理）。
3. **提示词多路径**：任何写作类要求都要检查 5.3 列出的全部注入点，否则只改一处
   不生效（生产走 Agent 路径）。字数契约已单点化（contract.txt），其余提示词
   改动先想「还有哪条路径也吃这段词」。
4. **排障顺序**：`docker logs qvp2-app`（或本地 uvicorn 输出）→ SSE
   `/api/stream/state`（含 error/debug/trace，比库里 error_class 详细）→
   创作网关会话日志：Nanobot 在 `~/.nanobot/workspace/sessions/*.jsonl`；
   dsh 在 `$DSH_HOME/sessions/**/session.jsonl.zstd`（zstd 压缩，用
   `dsh_serve/analyze_session.py` 解析——余额不足、max-tokens 截断这类真因都在这）。
5. **图上文案字数**：模型偶尔越界 ±5 字，硬校验会自动重试一次，仍越界保留较轻一版
   交人工审图兜底——不要为此加死循环。
6. **生图成本**：一条任务 6-8 张上限（配额硬限），余额/限流错误会以任务失败收场，
   报错现在会带 Agent 备注（1eb6c0b）。
7. **docker run 裸命令调 qvp3 镜像会触发 ENTRYPOINT**（entrypoint.dsh.sh 会起
   init_db→dsh_serve→后端一整套）——只想进容器调试必须
   `docker run --entrypoint sh ...`，否则端口/进程一团乱。
8. **MCP_COMMAND 必须全路径**：dsh_serve 按文件路径判定存在性
   （`/health` 的 `mcp.command_exists`），写 `python` 这种靠 PATH 解析的名字会
   判不存在、MCP 静默不挂；容器内用 `/usr/local/bin/python`，本地用
   `.venv/Scripts/python.exe` 全路径。
9. **node alpine 与 Debian glibc 不匹配**：`node:*-alpine` 是 musl libc，二进制
   COPY 进 python:3.12-slim（Debian）跑不起来（exit 127）——应用镜像必须用
   `node:24-slim` 做复制源（deploy/Dockerfile.dsh 头部注释有写）。
10. **execute_node 已改三段式短事务**（2026-09）：旧版一个事务跑完整个节点，
    长外部 I/O 期间连接被中间层掐断、收尾 commit 抛 InterfaceError 拖垮节点；
    现为「短事务落 started → 节点裸跑 → 新短事务回写 finished/failed」。
    取消语义随之改变：CancelledError 显式捕获后**删除本次 started 事件**
    （等价旧版"取消即回滚无残留"，重试会重新执行）；进程崩溃会残留 started
    事件（幂等层把未完成事件视为可重跑，删旧重建）——任务详情短暂显示
    running 属预期，不要误判为卡死。
11. **能力工具内嵌 LLM 调用叠加 Agent 外层推理**，单工具耗时可达 400+s——
    调用方超时需 ≥600s（生产链路 3000s 已够）；curl 超时丢响应不丢结果，
    同 session 追问可取回。

## 15. 近期改动史与待办 backlog

近期（2026-08~09）主线，详见 git log 与 docs/：
- 33bcffc 生图丰富度训练（横幅金句条/序号章/贴纸/高亮；新中式史料卡/政务办事指南卡风格）
- 9fefa33 正文真人感增强（_DRAFT_SHARED 三路径注入 + 信息密度段）
- 6a833e3 图上文案提密度（80-130 字七处同步）
- 1eb6c0b Agent 契约失败报错带备注
- ba0ce24+ **dsh 创作网关重构**（2026-09-07 本版基线）：
  Nanobot→dsh_serve 薄层（dsh 0.1.2-rc.1 钉版）、提示词 skill 化
  （skills/ 10 包 + contract.txt 字数契约单点）、execute_node 三段式短事务、
  qvp3(:8005) 灰度新栈上线；验证全记录 `docs/dsh-spike-验证记录.md`
- **生图升级 WS3+WS1+100% 标准**（2026-09-11，工作副本无 git 以本文档为准）：
  公共风格库单一事实来源 `data/styles.json` + `scripts/sync_styles.py`
  （init_db 末尾幂等同步，公共库改动一律改 json）；迁移 023 给 style_keywords
  加 use_when/pitfalls/source 三列（风格选型打分吃 use_when 权重减半，
  build_style_block 拼「适用/忌讳」段，Agent 判定段 2b 带完整信号）；
  错别字/异体字 100% 审核标准：图上文字质检由「相似度 ≥0.82」提升为
  「归一化（繁简/异体字映射 + 全半角统一 + 去空白，`src/quality/text_norm.py`）
  后逐字 100% 相等」，不一致触发重生成、仍不符交人工，不降级放行
- **生图升级 WS2+WS4**（2026-09-11）：WS2 模板蒸馏（借鉴
  .vendor/awesome-gpt-image-2）——data/styles.json 既有 27 条补齐
  use_when/pitfalls（蒸馏物 B），新增 6 条 oss_distilled 公共风格
  （爆炸拆解透视卡/时间轴演进卡/知识图谱关系卡/自然科普图鉴卡/
  报告手册排版卡/流程图解科普卡，蒸馏物 A），公共库达 33 条；
  蒸馏物 C（layouts 骨架句追加）按「verify 零差异」门禁放弃
  （追加必然改变渲染，构造上互斥，且底座已有等价约束）；
  WS4 基准集 `data/bench/bench_set.json`（20 题 = 开源蒸馏 8 + 历史真实 12）+
  `scripts/bench_image_quality.py`（投递→轮询终态→缩略图拼版快审，
  真实批需用户确认，联调走 MOCK）
- **灰度数据对比 + reasoningEffort 专项**（2026-09-14，详见
  `docs/qvp3-灰度数据对比-20260914.md`）：Nanobot 时代限流/余额故障
  （87/52 处）在 dsh 灰度期归零；`DSH_REASONING_EFFORT=medium` 实测被全部
  三级模型拒绝（不支持），调低推理档位路径不存在；推理挤占风险随主模型
  换 deepseek-v4-flash 基本消除（pro 时代极值 99% vs flash 可忽略），
  **若回切 pro 须把 max_tokens 提至 49152+ 并重验**；WS2 蒸馏物 C 用户决策
  不放行；WS4 真实批用户已批减半执行（10 题）

Backlog（未做）：
- ⚠ **test_agent_pipeline 5 项集成红（2026-09-14 发现，staged 重构遗留）**：
  test_agent_pipeline_produces_full_artifacts / test_agent_output_correction_round /
  test_agent_two_bad_rounds_fails_node / test_detail_node_timeline /
  test_agent_prompt_appends_draft_persona——基线 v12（36d9d9f）即红，与 9-14
  成本修复无关；P1 修复回归已验证零新增失败。修复时先对照 staged
  （agent_stages）路径的节点事件/契约重试语义。
- 两级风格库偏好闭环深化（个人库使用数据回流推荐）
- partial_regen Agent 化（当前定点重生成仍走直连提示词）
- ~~`IMAGE_GEN_CHANNELS` 加备胎~~（2026-09-10 已完成：fusion,linkai,moacode 轮询）
- ~~WS2 蒸馏物 C~~（**用户决策 2026-09-14：不放行**，维持按门禁放弃——
  与底座语义重叠且守卫零差异门禁构造上无法满足；素材库 .vendor 保留可捡）
- WS4 真实批：~~用户已批减半执行~~（2026-09-14 已完成：MOCK 20/20 +
  真实 10/10 全到 review；**成本 ¥46 超预算 1.5-1.8 倍**——100% OCR 质检致
  单任务 12-46 张重生成 + 8 张配额在 Agent 路径失效，详见
  `docs/qvp3-灰度数据对比-20260914.md` §7）
- **出图成本失控 P1 修复**（2026-09-14，针对上条，本地已验证待发版）：
  ① garble 重生提示词矛盾修正——原「文字只保留最核心一句 ≤20 字」提示词在
  9-11 的 100% 逐字质检标准下数学上必败（≤20 字对 80-130 字整页文案永远
  不可能全等），每次触发纯烧 2 张图零质量收益；改为「逐字复现原文案重排版」；
  ② 新增 `image_total` 任务级出图总预算（默认 14 张 = 6 交付 + 8 余量，
  `IMAGE_BUDGET_PER_TASK`）：MCP 首轮 / garble 重生 / 主体重画 / AI 审核重生
  四条出图路径统一累计记账，到顶停止自动重生、保留当前最佳版打标记进人工；
  ③ 配额累计语义——`task_quotas.reset` 不再返还 image_total，堵「节点重跑
  全额返还预算」漏洞（实测该漏洞致单任务 46 张 ≈ ¥9.2）；
  ④ `_GARBLE_MAX_REGEN` 2→1（与「自动重试一次」文档口径对齐）。
  守卫：`tests/unit/test_image_budget.py`。
  **P2 待拍板**：100% OCR 标准的 VL 申诉通道（OCR 误判经 qwen-vl-max 复核
  放行，放行口径不变）+ 首过率治理（统计首轮 sim 分布，对系统性低分风格加固）
- 前端无构建方案在视图变多后可考虑迁 Vite（可选）
- qvp3 灰度观察：长会话 compaction 行为与 Nanobot 不同，长任务输出稳定性待观察；
  32768 对更长任务仍可能被推理吃光（见 5.6 第 2 条残留风险）
- qvp2（Nanobot 栈）→ qvp3（dsh 栈）的生产切换评估（灰度稳定后）

## 16. 找谁/找什么

- 历史设计文档：`docs/改造说明-Nanobot全链Agent.md`（架构决策）、
  `docs/开发思路与工作架构.md`、`docs/风格库训练方法总结.md`（风格训练方法论）
- dsh 迁移：`docs/dsh-spike-验证记录.md`（Go/No-Go + 三轮补测铁证）、
  `dsh_serve/README.md`（配置全表/失败判定清单/遗留）
- 提示词演进：`src/gateway/prompt_versions.py` 与各 `skills/<name>/SKILL.md`
  的 notes 按日期可当 changelog 读
- 平台使用手册（运营视角）：web 端「样例」页与各视图内嵌说明
