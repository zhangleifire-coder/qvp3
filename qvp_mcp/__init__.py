"""QVP MCP 工具服务器：供 Nanobot 创作 Agent 调用的外部能力工具集。

设计原则（对应改造方案「软件工程层兜底」）：
- 工具实现全部复用 src/gateway 现有网关（同一套 .env、同一套上游 API 行为）；
- 每个 task_id 有硬配额（生图 ≤ N 张 / 搜索 ≤ N 次 / OCR ≤ N 张），
  超 limit 直接报错——不依赖 LLM 自律，防 Agent 失控烧钱；
- 每次工具调用把成本异步回调给业务后端记账（node_events 成本口径不变）；
- MCP 进程不做任何数据库操作：DB 写入永远归属后端（职责边界）。
"""
