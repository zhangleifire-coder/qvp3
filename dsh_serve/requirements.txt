# dsh_serve 组件依赖 —— 需合并进主 requirements.txt（或直接 pip install -r 本文件）
# dsh 版本钉死：spike 实测版本 0.1.2-rc.1（pypi 版本号写法 0.1.2rc1），
# SDK 会自动安装同版本 deepseek-harness-runtime-bin（自带 dsh CLI，无需系统 Node）。
deepseek-harness-sdk==0.1.2rc1
fastapi>=0.115
uvicorn>=0.30
pydantic-settings>=2.4
pyyaml>=6.0

# --- 仅单测需要 ---
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
